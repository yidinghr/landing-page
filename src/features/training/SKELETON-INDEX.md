# Operation Training — Skeleton Index

This file is the entry point for any AI agent implementing the redesign.
Read this before touching any file.

---

## New files created (skeletons, need implementation)

| File | Phase | Status | What AI must do |
|---|---|---|---|
| `phase-machine.js` | Phase1 | ✅ Complete | No changes needed — use as-is |
| `training-state.js` | Phase1 | 🟡 Skeleton | Verify mutation functions are wired into orchestrator |
| `training-orchestrator.js` | Phase1 | 🟡 Skeleton | Migrate logic from training-controller.js into handlers |
| `engines/dealing-validator.js` | Phase7 | 🟡 Skeleton | Cases already stubbed — verify edge cases and test |
| `engines/payout-validator.js` | Phase9 | 🟡 Skeleton | Implement isSettlementComplete wiring |
| `ui/drag-engine.js` | Phase6/9 | 🟡 Skeleton | Implement initCardDrag and initChipDrag |
| `ui/reveal-flow-manager.js` | Phase8 | 🟡 Skeleton | Implement buildRevealQueue, validateFlipAction |
| `ui/result-boards-renderer.js` | Phase4 | 🟡 Skeleton | Implement buildBigRoadData fully, derived roads |
| `ui/card-counter-renderer.js` | Phase5 | 🟡 Skeleton | Verify renderCardCounter and renderLiveProb |
| `npc/npc-request-engine.js` | Phase10 | 🟡 Skeleton | Implement generateRoundRequests fully |

## Existing files with extensions added

| File | Change | Phase |
|---|---|---|
| `engines/prob-engine.js` | Added `probFromShoe()` stub | Phase5 |

## Existing files — DO NOT modify without explicit phase instruction

| File | Why protected |
|---|---|
| `engines/baccarat-engine.js` | Correct pure logic — no changes needed ever |
| `engines/shoe-engine.js` | Correct pure logic — no changes needed |
| `engines/payout-engine.js` | Correct pure logic — no changes needed |
| `engines/insurance-engine.js` | Working correctly — extend only in Phase12 |
| `engines/settlement-engine.js` | Working correctly — do not change row shape |
| `engines/seat-engine.js` | Working correctly |
| `config/config-manager.js` | localStorage keys must not be renamed |
| `npc/npc-behavior.js` | Keep npcAutoBet intact — only add request hooks |
| `scenarios/shoe-presets.js` | Keep as-is |
| `scenarios/wrong-payout.js` | Keep as-is |
| `controllers/` (3 files) | Keep guard logic as-is |

## Phase execution order

```
Phase 1  → training-state.js + training-orchestrator.js wired into training-controller.js
Phase 2  → home/training/index.html layout + assets/css/training.css
Phase 3  → renderer reconnection (update DOM IDs in table-renderer.js)
Phase 4  → result-boards-renderer.js (roadmaps)
Phase 5  → card-counter-renderer.js + probFromShoe in prob-engine.js
Phase 6  → drag-engine.js initCardDrag + training-orchestrator.handleCardDrop wired
Phase 7  → dealing-validator.js wired into orchestrator.handleCardDrop
Phase 8  → reveal-flow-manager.js + card flip mechanics in HTML/CSS
Phase 9  → drag-engine.js initChipDrag + payout-validator.js
Phase 10 → npc-request-engine.js wired into orchestrator + speech bubbles in HTML
Phase 11 → customer request panel in HTML + wired to revealQueue
Phase 12 → insurance config extensions in config-manager.js + settings-panel.js
Phase 13 → QA pass — no new features
```

## Key invariants to preserve

- `training-controller.js` must get SMALLER with every phase, not larger
- All engines remain pure functions (no DOM, no state)
- `log[]` array is newest-first — reverse it when building road data
- Auto-deal button must remain working throughout all phases (testing bypass)
- localStorage keys in config-manager.js must NOT be renamed
- `settlement-engine.settleRound()` output shape must NOT change

## How to verify a phase is done

1. `npm run build` passes with zero errors
2. Full manual round: idle → deal 4 cards → reveal → settlement → next → idle
3. Insurance still triggers with `offerCondition: 'always'`
4. Wrong-payout drill still shows and catchable
5. Auto-deal mode still completes a full round without drag interaction
6. No console errors during a 5-round session

---

*Generated by skeleton builder — do not delete this file*
